Event photographs referenced by `constants/gallery.ts` (drives, training
sessions, internship orientation, offer-letter distribution).

Naming convention, sizing, and alt-text rules: see `../ASSET_GUIDE.md`.

Each file added here must have a matching entry in `constants/gallery.ts`
with `status: "verified"` — do not leave demo entries pointing at files
that don't exist once real photographs are added.
